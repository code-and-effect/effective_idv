class CreateEffectiveIdv < ActiveRecord::Migration[6.0]
  def change

    create_table <%= @identity_verifications_table_name %> do |t|
      t.string :identity_verification_type

      t.string :user_type
      t.integer :user_id

      t.date :expiry_date

      # Lockbox gem
      t.text :legal_name_ciphertext
      t.text :date_of_birth_ciphertext

      t.string :status
      t.string :status_steps
      t.text :wizard_steps
      t.string :token

      t.datetime :submitted_at
      t.datetime :approved_at

      t.datetime :declined_at
      t.text :declined_reason

      t.text :notes

      t.timestamps
    end

  end
end
